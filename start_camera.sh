
# 启动第一个 follow1 文件
# gnome-terminal -- bash -c "cd /media/dc/HP2024/dp_inference/follow2 && source devel/setup.bash && roslaunch arm_control arx5.launch"

gnome-terminal -- bash -c "cd /media/dc/HP2024/dp_inference/follow1 && source devel/setup.bash && roslaunch arm_control camera.launch"
